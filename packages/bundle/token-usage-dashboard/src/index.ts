/**
 * @deepseek-ai/dsh-token-usage-dashboard — the Token consumption dashboard as
 * a profile bundle. The package's substance is `cordis.patch.yml`, declared by
 * the `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-token-usage-dashboard
 */

export {}
